package com.adfonic.domain;

/**
 * These are modes in which beacons may be rendered in ad responses.
 */
public enum BeaconMode {
    MARKUP, // rendered as <img> in the HTML markup
    METADATA // rendered as separate components/metadata
}
