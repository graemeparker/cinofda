#if($adtruthjs)
    #parse($adtruthjs)
#end
#if($adComponents.components.image)
<!-- 1024x90 image -->
<a href="${adComponents.destinationUrl}"><img border="0" src="${adComponents.components.image.url}"#if($adComponents.components.image.width) width="${adComponents.components.image.width}"#end#if($adComponents.components.image.height) height="${adComponents.components.image.height}"#end /></a>
#end

#if($renderBeacons && $adComponents.components.beacons)
#set($Integer = 0)
#foreach ($idx in [1..$Integer.parseInt($adComponents.components.beacons.numBeacons)])
<!-- Beacon ${idx} -->
#if($adtruthjs)
    <script>
            document.write('<img width=1 height=1 src="#evaluate("$adComponents.components.beacons.beacon$idx")d.adtruth_data=' + adtruth_data + '"/>');
    </script>
    <noscript>
        <img width=1 height=1 src="#evaluate("$adComponents.components.beacons.beacon$idx")"/>
    </noscript>
#else
    <img width=1 height=1 src="#evaluate("$adComponents.components.beacons.beacon$idx")"/>
#end

#end
#end
